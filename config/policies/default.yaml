# Default policy configuration
# enforcement_mode options: blocking, warning, advisory
# blocking: Blocks merge on critical/high violations
# warning: Allows merge but shows warnings
# advisory: Only reports violations, always allows merge
enforcement_mode: blocking
severity_threshold: medium
allow_blocking_override: true

enabled_rules: []
disabled_rules: []

rule_packs:
  - default

custom_rules: []
