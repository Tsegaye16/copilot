fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
httpx
pyyaml
google-generativeai
python-multipart
aiofiles

# Optional dependencies (commented out - uncomment if needed)
# sqlalchemy==2.0.23  # For database (if using PostgreSQL)
# alembic==1.12.1     # For database migrations
# psycopg2-binary==2.9.9  # PostgreSQL driver
# redis==5.0.1        # For caching (if using Redis)
# celery==5.3.4       # For background tasks (if needed)
# python-jose[cryptography]==3.3.0  # For JWT auth (if needed)
# passlib[bcrypt]==1.7.4  # For password hashing (if needed)
# pytest==7.4.3      # For testing
# pytest-asyncio==0.21.1  # For async testing
